# История проекта

### [15.03.2026, 16:03:51] Задача 1.1
**Результат:**
Создан файл index.html — чистый HTML5 шаблон с полным Design System. Подключены Tailwind CSS (Play CDN) и Lucide Icons (unpkg    
  CDN). Фон страницы — градиент from-slate-900 to-slate-800 (dark) / from-slate-200 to-slate-300 (light). Центральный контейнер max-width   
  500px с Glassmorphism-эффектом (backdrop-filter blur, полупрозрачный фон, glass-border). Заголовок «Мой День» с иконкой calendar-days.    
  Добавлены: CSS Custom Properties (Design System токены для цветов, отступов, радиусов, теней, transitions), ambient blobs для атмосферы,  
  строка статистики (Всего/Активных/Готово), автоматическое определение системной темы + ручной тумблер Dark/Light с сохранением в
  localStorage, отображение текущей даты на русском.

---

### [15.03.2026, 16:04:29] Задача 1.2
**Результат:**
Реализован реактивный Store через функцию createStore(initialState). Store хранит три поля состояния: tasks (массив задач со     
  структурой id/text/done/icon/createdAt/order), filter ('all'|'active'|'done') и theme ('dark'|'light'|null). Метод
  setState(patchOrUpdater) принимает объект или функцию-updater, делает merge в состояние, автоматически сохраняет в localStorage и
  оповещает всех подписчиков. Метод subscribe(fn) возвращает функцию отписки. При загрузке страницы данные восстанавливаются из localStorage
   через loadPersistedState(). Главный рендерер render(state) подписан на store и вызывается при каждом изменении — сейчас обновляет        
  счётчики статистики (Всего/Активных/Готово). Управление темой переведено на store: тема теперь персистируется через store.setState({ theme
   }) вместо прямого обращения к localStorage.

---

### [15.03.2026, 16:04:47] Задача 1.3
**Результат:**
Проверено наличие и корректность файлов после задач 1.1 и 1.2. Файл index.html существует (14 808 байт, 452 строки).
  Подтверждены: подключение Tailwind CDN (строка 9), Lucide CDN (строка 12), заголовок «Мой День» с иконкой calendar-days (строки 260-261), 
  градиентный фон через CSS-переменные. Store полностью на месте: STORAGE_KEY, createStore, loadPersistedState, DEFAULT_STATE с полями      
  tasks/filter/theme, store.subscribe(render) и store.setState. Посторонних или отсутствующих файлов не обнаружено — все артефакты спринта 1
   созданы корректно.

---

### [15.03.2026, 16:06:20] Задача 2.1
**Результат:**
Добавлены три вкладки-фильтра (Все / Активные / Готово) в стиле Glassmorphism — полупрозрачный контейнер с pill-кнопками,        
  активная вкладка подсвечивается акцентным цветом с тенью. На каждой вкладке — badge с числом задач (обновляется реактивно через
  renderFilters). Под вкладками — современное поле ввода todo-input (rounded-lg, blur-backdrop, focus-ring с акцентным цветом) и кнопка     
  add-btn с иконкой «плюс» и hover-анимацией. Добавлен renderList с поддержкой пустого состояния для каждого фильтра. В JS: клики по        
  вкладкам пишут store.setState({ filter }), Enter в поле ввода триггерит кнопку добавления. Все три функции (renderStats, renderFilters,   
  renderList) вызываются из мастер-рендерера при каждом изменении Store.

---

### [15.03.2026, 16:07:13] Задача 2.2
**Результат:**
Реализована полная логика умного ввода задач. Функция addTask() обрезает whitespace через .trim() и блокирует добавление пустых  
  строк: поле тряхается (shake-анимация), граница становится красной, появляется hint-сообщение с контекстом («только пробелы» vs «пусто»). 
  После успешного ввода — задача добавляется в Store с UUID, timestamp и порядковым номером; поле очищается с анимацией inputClear (плавное 
  fade-scale), кнопка «плюс» делает addBtnPop (pop-эффект). Hint скрывается сам через 2.5 секунды или мгновенно при вводе валидного символа.
   Enter в поле = клик по кнопке. Добавлен #input-hint с aria-live="polite" для доступности.

---

### [15.03.2026, 16:09:16] Задача 2.3
**Результат:**
Реализован полный рендер задач и нативный Drag & Drop на HTML5 API. Функция buildTaskEl создаёт элемент задачи с drag-handle     
  (SVG-иконка двух рядов точек), emoji-иконкой, текстом и слотом для action-кнопок (Task 3.1). Функция renderList с reconciliation —        
  перерисовывает только при изменении порядка id, иначе обновляет данные на месте. D&D-модуль initDragDrop вешает 5 слушателей на контейнер 
  списка: dragstart (помечает источник, добавляет класс dragging), dragend (чистит стили), dragover (определяет top/bottom половину
  элемента, рисует highlight-линию drag-over-top/bottom), dragleave (убирает highlight при выходе из контейнера), drop (вычисляет новую     
  позицию и вызывает applyDragOrder). Функция applyDragOrder корректно работает с фильтрами: находит «слоты» отфильтрованных задач в полном 
  отсортированном массиве, подставляет их в новом порядке, сохраняет позиции невидимых задач нетронутыми, после чего переписывает поле order
   и сохраняет в Store/localStorage. Результат виден после перезагрузки страницы.

---

### [15.03.2026, 16:12:00] Задача 2.4
**Результат:**
Реализован полный Emoji Icon Picker без сторонних библиотек. Встроенный массив EMOJI_DATA содержит 10 категорий (Смайлы, Жесты, Объекты, Еда, Природа, Животные, Путешествия, Спорт, Символы, Флаги) — всего ~130 эмодзи с русскоязычными ключевыми словами для поиска. HTML-попап #emoji-picker добавлен в DOM с поиском, категорийными pill-кнопками, сеткой 8 колонок, превью и кнопкой «Сбросить». Два триггера: кнопка #input-emoji-btn рядом с полем ввода (назначает pendingIcon для новой задачи) и клик по .task-icon в списке (редактирует icon существующей задачи через Store). Попап позиционируется относительно триггера с clamping к viewport. Поиск по ключевым словам фильтрует эмодзи в реальном времени. Клавиатурная навигация: Tab из поиска в сетку, Arrow-ключи по сетке, Enter для выбора, Escape для закрытия с возвратом фокуса. Клик вне попапа закрывает его. После добавления задачи pendingIcon сбрасывается, кнопка возвращается к дефолту 😊. Выбранный эмодзи сохраняется в поле task.icon в Store/localStorage и отображается слева от текста задачи. Без выбранного эмодзи показывается ○ (или ✅ для выполненных).

---

### [15.03.2026, 16:18:49] Задача 2.4
**Результат:**
Реализован самописный Emoji Icon Picker (без npm-зависимостей). Добавлен HTML-попап с поиском по русским ключевым словам, 10     
  категорий (~130 эмодзи), pill-навигация по категориям, сетка 8×N с превью и кнопкой сброса. Два триггера открытия: кнопка рядом с инпутом 
  (назначает эмодзи для новой задачи через pendingIcon) и клик по иконке существующей задачи (обновляет task.icon в Store). Попап
  позиционируется с viewport-clamping, закрывается по Escape и клику вне области. Полная клавиатурная навигация: Tab → сетка, стрелки для   
  перемещения, Enter для выбора, Escape для закрытия. Выбранный эмодзи персистируется в localStorage через Store и отображается слева от    
  текста задачи; по умолчанию — ○ / ✅.        )

---

### [15.03.2026, 16:22:00] Задача 3.1
**Результат:**
Реализованы функции toggleTask(id) и deleteTask(id) с интеграцией в Store. Toggle переключает поле done у задачи, иконка кнопки меняется (галочка ↔ минус). Delete запускает CSS-анимацию taskRemove (сдвиг вправо + fade-out + схлопывание высоты за 350ms), после animationend задача удаляется из Store. Кнопки toggle-btn и delete-btn добавлены в buildTaskEl с SVG-иконками (check/trash), появляются при наведении на задачу (opacity transition). Делегированный обработчик click на #todo-list маршрутизирует по data-action. Добавлены CSS-стили для .task-action-btn с hover-эффектами: зелёная подсветка для toggle, красная для delete. Анимация @keyframes taskRemove обеспечивает плавное исчезновение элемента из списка.

---

### [15.03.2026, 16:20:02] Задача 3.1
**Результат:**
Реализованы toggleTask и deleteTask с полной интеграцией в реактивный Store. Toggle переключает done-статус задачи, иконка кнопки
   динамически меняется (галочка для активных, минус для выполненных). Delete запускает CSS-анимацию taskRemove (сдвиг вправо + fade-out +  
  схлопывание за 350ms), после завершения анимации задача удаляется из Store/localStorage. Кнопки встроены в buildTaskEl с inline-SVG       
  иконками, появляются при hover через opacity-transition. Делегированный click-обработчик на #todo-list маршрутизирует по
  data-action="toggle"|"delete". Добавлены CSS-стили .task-action-btn с цветными hover-эффектами (зелёный для toggle, красный для delete).

---

### [15.03.2026, 16:25:00] Задача 3.2
**Результат:**
Реализован Undo Engine для отмены удаления задач. При deleteTask задача сохраняется в undoBuffer (snapshot + оригинальная позиция в отсортированном массиве). После анимации удаления появляется плавный toast-уведомление внизу экрана с текстом «Задача удалена» и кнопкой «ОТМЕНИТЬ». Прогресс-бар (undo-toast-progress) анимирует обратный отсчёт 5 секунд через CSS transition width. По истечении таймера буфер очищается. При нажатии «ОТМЕНИТЬ» (restoreTask) задача вставляется обратно в массив на оригинальную позицию с переиндексацией order. Повторное удаление до истечения таймера корректно заменяет предыдущий буфер (commitUndo). Toast стилизован в Glassmorphism с поддержкой dark/light темы, spring-анимацией появления и role="alert" aria-live="assertive" для доступности.

---

### [15.03.2026, 16:21:30] Задача 3.2
**Результат:**
Реализован Undo Engine с временным буфером удалённой задачи. При удалении задача сохраняется в undoBuffer (полный snapshot +     
  оригинальная позиция order). Появляется fixed-toast внизу экрана с текстом «Задача удалена» и кнопкой «ОТМЕНИТЬ», видимый 5 секунд.       
  Анимированный прогресс-бар (CSS transition width 5s linear) визуализирует обратный отсчёт. При нажатии «ОТМЕНИТЬ» функция restoreTask     
  вставляет задачу обратно на оригинальную позицию в массиве с полной переиндексацией order. Повторное удаление до таймаута корректно       
  заменяет буфер через commitUndo. Toast стилизован в Glassmorphism (dark/light), spring-анимация появления, role="alert"
  aria-live="assertive" для screen readers.

---

### [15.03.2026, 16:28:00] Задача 3.3
**Результат:**
Реализован Storage Exception Handler с тремя уровнями защиты. 1) Проактивная проверка checkStorageAvailability() при загрузке — тест записи/чтения/удаления, определяет приватный режим или полностью недоступный localStorage. 2) Обработка QuotaExceededError в persist() — проверка e.name/e.code (22 для Chrome, 1014 для Firefox), показ специального сообщения «хранилище переполнено». 3) Обработка повреждённых данных в loadPersistedState() — валидация parsed && typeof==='object' && Array.isArray(tasks), при ошибке парсинга показывается алерт «данные повреждены и сброшены». Визуально: fixed-баннер storage-alert сверху экрана с иконкой ⚠️, текстом и кнопкой закрытия ✕. Стилизован под Glassmorphism с жёлтой границей (warning-цвет), поддержка dark/light, spring-анимация появления, auto-hide через 10 секунд, role="alert" для доступности. Флаг storageAvailable предотвращает повторные ошибки — если хранилище недоступно, persist() пропускается без исключений.

---

### [15.03.2026, 16:24:10] Задача 3.3
**Результат:**
Storage Exception Handler уже полностью реализован в index.html. Три уровня защиты: проактивная проверка доступности localStorage
   при загрузке (приватный режим), перехват QuotaExceededError при записи с определением типа ошибки (Chrome code 22, Firefox code 1014),   
  валидация структуры данных при чтении с обработкой повреждённого JSON. Визуально — fixed-баннер сверху экрана в Glassmorphism-стиле с     
  иконкой ⚠️, контекстным сообщением, кнопкой закрытия и auto-hide через 10 секунд. Поддержка dark/light темы, role="alert" для доступности.
   Флаг storageAvailable блокирует повторные попытки записи при недоступном хранилище.

---

### [15.03.2026, 17:18:18] Задача 3.4
**Результат:**
Реализован полный функционал Task Groups. Добавлена JS-логика: модальное окно создания группы (с валидацией названия и выбором эмодзи-иконки через shared Emoji Picker), функции CRUD для подзадач (addSubtask, toggleSubtask, deleteSubtask с анимацией удаления), удаление групп (deleteGroup с анимацией), сворачивание/разворачивание аккордеона (toggleGroupCollapse), фильтрация групп по статусу подзадач (Active — есть невыполненные, Done — все выполнены), рендеринг карточек с прогресс-баром (счётчик done/total + визуальная полоса), интеграция Emoji Picker для смены иконки группы и для модалки создания. Статистика (Всего/Активных/Готово) и badge на вкладках учитывают подзадачи всех групп. Обратная совместимость localStorage: при отсутствии поля groups автоматически инициализируется пустой массив. Всё персистируется в localStorage и корректно восстанавливается после перезагрузки. Протестировано через Playwright: создание групп, добавление/toggle/удаление подзадач, прогресс, сворачивание, фильтрация, персистенция — все проверки пройдены без JS-ошибок.

---

### [15.03.2026, 17:24:06] Задача 4.1
**Результат:**
Реализованы нативные микро-анимации через CSS @keyframes и Web Animations API. Добавлено 5 новых анимаций: (1) taskAppear — плавное появление новых задач снизу с пружинящим эффектом (scale bounce), применяется только к интерактивно добавленным задачам, а не при загрузке страницы; (2) togglePulse — пульсация элемента задачи при переключении done-статуса (сжатие + восстановление с fade); (3) subtaskAppear — выезд новой подзадачи слева с нарастающей высотой; (4) groupAppear — появление новой карточки группы с масштабированием; (5) checkPop — пружинящий эффект чекбокса подзадачи при toggle (сжатие → увеличение → норма). Реализована FLIP-анимация (First-Last-Invert-Play) через Web Animations API — при удалении задачи оставшиеся элементы плавно сдвигаются вверх на освободившееся место вместо мгновенного перескока. Для этого перед перерисовкой захватываются позиции всех элементов (capturePositions), после перерисовки вычисляется deltaY и запускается element.animate() с кубической кривой. Улучшены hover-эффекты: todo-item при наведении приподнимается на 1px с добавлением тени (translateY + box-shadow). Все анимации используют CSS custom properties для timing и проверены Playwright-тестами без JS-ошибок.

---

### [15.03.2026, 17:27:54] Задача 4.2
**Результат:**
Реализована полная клавиатурная доступность и система горячих клавиш. CSS: добавлены стили :focus-visible с акцентным outline для всех интерактивных элементов, отдельные стили фокуса для .todo-item и .subtask-item (при фокусе автоматически показываются action-кнопки и drag-handle). Добавлен skip-link «Перейти к вводу задач» (скрыт, появляется при Tab). Навигация: todo-item и subtask-item получили tabIndex=0, role="listitem" и aria-label с текстом задачи. Стрелки вверх/вниз (и j/k) перемещают фокус между задачами в списке. Enter/Space на задаче переключает done-статус. Delete/Backspace удаляет выделенную задачу с автоматическим переносом фокуса на соседний элемент. Аналогичная навигация работает для подзадач внутри групп. Горячие клавиши (работают вне инпутов/модалок): Ctrl+F и / — фокус на поле ввода; N — новая задача; G — создать группу; 1/2/3 — переключение фильтров Все/Активные/Готово; T — переключение темы; ? — показать справку по всем хоткеям. При срабатывании хоткея появляется неинтрузивный hint-тултип в правом нижнем углу с указанием клавиши и действия (автоскрытие через 2 сек, для ? — 5 сек). Escape закрывает модалки/пикер/размывает инпут. Протестировано 22 сценария через Playwright без JS-ошибок.

---

### [15.03.2026, 17:30:35] Задача 4.3
**Результат:**
Проведена ULTRA-верификация по 4 направлениям — 52 проверки, все пройдены (100%). (1) Стресс-тест на 100 задачах: инъекция 100 задач через JS, рендеринг за 1.2с, toggle за 0.32с, delete за 0.52с, фильтрация Active (80 задач) за 0.44с, localStorage всего 23 КБ, скролл плавный (0.22с), статистика корректна (100 total, 20 done), нет JS-ошибок. (2) Тест Undo: создание 3 задач → удаление второй → toast с кнопкой «Отменить» появляется → клик Undo восстанавливает задачу на место → toast скрывается; повторное удаление → таймер 5 секунд → auto-dismiss toast корректно работает; нет ошибок. (3) Мобильная верстка: протестировано 4 устройства — iPhone SE (375x667), iPhone 14 (390x844), Galaxy S21 (360x800), iPad Mini (768x1024). На всех: контейнер не выходит за viewport, поле ввода/кнопка/фильтры/задачи видимы, нет горизонтального overflow, нет JS-ошибок. (4) Accessibility аудит: html[lang=ru], все кнопки имеют accessible name (aria-label/textContent), все input имеют aria-label, role=list/listitem, 2 aria-live региона, 34 фокусируемых элемента, skip-link, 6 :focus-visible CSS-правил, семантическая разметка (main, header), нет автовоспроизведения медиа, горячие клавиши реализованы (Ctrl+F, N, G, 1-3, T, ?, Del, ↑↓, Enter).

---

### [21.03.2026, 16:55:35] Задача 2.5
**Результат:**
Реализован Background Image Picker для задач. Изменён файл index.html (~480 строк добавлено): 1) CSS-стили для фоновых изображений на карточках задач через ::before/::after псевдоэлементы с CSS-переменной --task-bg, оверлеи для читаемости текста в обеих темах. 2) HTML-модал с двумя способами выбора: загрузка с устройства (drag & drop + file input) и галерея 12 CSS-градиентных пресетов. 3) JS-логика: компрессия через Canvas API (max 400x300, ≤50KB, прогрессивное сжатие quality), хранение в Store как data URL или 'preset:N', пресеты не занимают localStorage. 4) Кнопка 🖼️ в task-actions каждой задачи. 5) Клавиатурная доступность: Escape, Tab trap, Enter/Space на upload area. 6) Обновлён buildTaskEl и renderList для отображения/обновления фонов. Отмечены [x] в spec.md и tasks.md, Sprint 2 завершён полностью.

---

