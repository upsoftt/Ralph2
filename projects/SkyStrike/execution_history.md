# История проекта

### [14.03.2026, 15:33:19] Задача 1.1
**Результат:**
Созданы файлы index.html и style.css. В HTML добавлен canvas с логическим размером 1080x1920 и UI-слой. В CSS настроено
  центрирование canvas (Letterboxing) с использованием свойства aspect-ratio 9:16 и max-height

 ⠙  (esc to cancel, 34s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.1</task_id>
  <summary>Созданы файлы index.html и style.css. В HTML добавлен canvas с логическим размером 1080x1920 и UI-слой. В CSS настроено
  центрирование canvas (Letterboxing) с использованием свойства aspect-ratio 9:16 и max-height

 ⠹  (esc to cancel, 34s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.1</task_id>
  <summary>Созданы файлы index.html и style.css. В HTML добавлен canvas с логическим размером 1080x1920 и UI-слой. В CSS настроено
  центрирование canvas (Letterboxing) с использованием свойства aspect-ratio 9:16 и max-height

 ⠹  (esc to cancel, 34s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.1</task_id>
  <summary>Созданы файлы index.html и style.css. В HTML добавлен canvas с логическим размером 1080x1920 и UI-слой. В CSS настроено
  центрирование canvas (Letterboxing) с использованием свойства aspect-ratio 9:16 и max-height: 100vh на черном фоне. UI-слой позиционирован
  абсолютно поверх холста.

---

### [14.03.2026, 15:34:41] Задача 1.2
**Результат:**
Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠸  (esc to cancel, 34s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠸  (esc to cancel, 34s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠼  (esc to cancel, 34s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠴  (esc to cancel, 34s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠦  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠦  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠧  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠇  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠇  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠏  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчиты

 ⠋  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчитывается deltaTime в секундах, с ограничением максимума в 0.1 сек для защиты от багов физики при переключении вкладок. Реализована  
  обработка массива entities с фильтрацией объектов, помеченных флагом markedForDeletion. В `main.js

 ⠋  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчитывается deltaTime в секундах, с ограничением максимума в 0.1 сек для защиты от багов физики при переключении вкладок. Реализована  
  обработка массива entities с фильтрацией объектов, помеченных флагом markedForDeletion. В `main.js

 ⠋  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчитывается deltaTime в секундах, с ограничением максимума в 0.1 сек для защиты от багов физики при переключении вкладок. Реализована  
  обработка массива entities с фильтрацией объектов, помеченных флагом markedForDeletion. В `main.js

 ⠙  (esc to cancel, 35s)
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
 *   Type your message or @path/to/file
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
✦ <result>
  <task_id>1.2</task_id>
  <summary>Созданы файлы src/Engine.js и src/main.js. Реализован класс Engine, управляющий основным игровым циклом requestAnimationFrame.   
  Рассчитывается deltaTime в секундах, с ограничением максимума в 0.1 сек для защиты от багов физики при переключении вкладок. Реализована  
  обработка массива entities с фильтрацией объектов, помеченных флагом markedForDeletion. В main.js настроена инициализация движка и вывод  
  показателя FPS в консоль через 3 секунды после старта. Файл index.html обновлен для подключения модуля main.js.

---

### [14.03.2026] Задача 2.1
**Результат:**
Создан и доработан файл `src/Player.js`. Класс `Player` содержит `type = 'player'`, `hp = 3`, `markedForDeletion = false`. Реализован обработчик ввода через `setupInput()` — флаги WASD/Стрелки + Space в объекте `keys`. Движение применяется в `update(dt)` с учётом `deltaTime`. Жёсткое ограничение по границам логического экрана (0..1080 по X, 0..1920 по Y) через clamp по полуширине/полувысоте. Отрисовка — процедурный треугольник-самолёт (голубой корпус, оранжевое свечение двигателя, блик кабины) средствами Canvas API с использованием `ctx.save/translate/restore`.

---

### [14.03.2026, 23:05:39] Задача 2.1
**Результат:**
ОПИШИ ЧТО БЫЛО 
 СДЕЛАНО

---

### [14.03.2026, 23:07:32] Задача 2.2
**Результат:**
ОПИШИ ЧТО БЫЛО
 СДЕЛАНО

---

### [14.03.2026] Задача 3.1
**Результат:**
Файлы `src/Enemies.js` и `src/WaveDirector.js` уже существовали с корректной реализацией. Класс `Scout` (`type = 'enemy'`, `hp = 2`) движется вниз со скоростью 300 px/s по синусоиде и помечается `markedForDeletion` при Y > 2000. Класс `WaveDirector` (`type = 'director'`) каждые 3 секунды спавнит 3 Scout-а из Y = -100. В `main.js` добавлен импорт и инстанцирование `WaveDirector`.

---

### [14.03.2026] Задача 3.2
**Результат:**
Создан файл `src/CollisionManager.js` с классом `CollisionManager` (`type = 'system'`, без отрисовки). В методе `update()` реализована строгая фильтрация по полю `type` (player, enemy, player_projectile) и проверка пересечения окружностей (`_circlesOverlap`): снаряды игрока уничтожают врагов, уменьшая их HP; при HP <= 0 враг удаляется; врagi при столкновении с игроком наносят ему урон и уничтожаются. В `main.js` добавлен импорт и инстанцирование `CollisionManager`.

---

### [14.03.2026] Задача 4.1
**Результат:**
Создан `src/ParticleSystem.js` с классом `ParticleSystem` (`type = 'system'`) и внутренним классом `Particle`. Каждая частица имеет случайный угол/скорость, физику гравитации, затухающую прозрачность и ограничение в 500 активных частиц (лишние вытесняют старые). `CollisionManager` обновлён — принимает `particleSystem` и вызывает `spawnExplosion()` при попаданиях (малый взрыв при уроне, большой при уничтожении врага, синий при смерти игрока). В `main.js` добавлен импорт и инстанцирование `ParticleSystem` с передачей в `CollisionManager`.

---

### [14.03.2026] Задача 4.2
**QA-отчёт (статический анализ кода):**

```json
{
  "scaling": "PASS — CSS aspect-ratio 9/16 + max-height:100vh, Letterboxing корректен",
  "player_alive_on_start": "PASS — CollisionManager фильтрует по type, self-collision невозможен",
  "enemies_spawn": "PASS — WaveDirector спавнит 3 Scout каждые 3 сек из Y=-100",
  "bullets_deleted": "PASS — PlayerProjectile.markedForDeletion=true при Y < -50",
  "particles_work": "PASS — ParticleSystem.spawnExplosion вызывается из CollisionManager",
  "memory_leak_risk": "FIXED — Engine.update теперь снимает snapshot перед итерацией",
  "hud_hp": "FIXED — #hp обновляется через hudLoop каждый кадр",
  "qa_logging": "ADDED — entities.length и particles.length логируются каждые 10 сек"
}
```

**Найденные и исправленные баги:**
1. `Engine.js`: `for...of` итерировал живой массив — `WaveDirector` мог добавлять врагов в текущий проход. Исправлено: `this.entities.slice()` перед итерацией.
2. `main.js`: HUD-элемент `#hp` был статичным. Добавлен `hudLoop()` через `requestAnimationFrame`.
3. `main.js`: Добавлено QA-логирование `entities.length` и `particles.length` каждые 10 сек для проверки отсутствия утечек памяти.

---

### [14.03.2026, 23:27:50] Задача 4.2
**Результат:**
Здесь напиши своими словами 2-3 предложения о том, что конкретно ты сделал:
какие файлы создал/изменил и какую функциональность реализовал

---

### [15.03.2026] Задача 5.1
**Результат:**
Создан файл `src/PowerUps.js` с базовым классом `PowerUp` (`type = 'powerup'`, падение 150 px/s, пульсирующая отрисовка цветных квадратов 40x40) и тремя наследниками: `ShieldPowerUp` (голубой, щит на 5 сек — поглощает 1 удар), `DoubleFirePowerUp` (красный, удвоение скорострельности на 8 сек через halving cooldown), `HealPowerUp` (зелёный, +1 HP до максимума 3). В `Player.js` добавлены таймеры `shieldTimer` и `doubleFireTimer`, логика уменьшения cooldown при активном DoubleFire, и отрисовка голубого кольца щита. В `WaveDirector.js` добавлен спавн случайного усилителя каждые 12±3 сек с лимитом 3 штуки на экране. В `CollisionManager.js` добавлена коллизия `player` vs `powerup` (вызов `applyTo`) и обработка щита при столкновении с врагом (щит поглощает удар и обнуляется). Повторный подбор Shield/DoubleFire сбрасывает таймер (не стакается).

---

### [15.03.2026, 17:09:27] Задача 5.1
**Результат:**
Создан `src/PowerUps.js` с базовым классом `PowerUp` (`type = 'powerup'`, падение 150 px/s, пульсирующая анимация) и тремя наследниками: `ShieldPowerUp` (голубой, щит на 5 сек — поглощает 1 удар), `DoubleFirePowerUp` (красный, удвоение скорострельности на 8 сек через halving cooldown), `HealPowerUp` (зелёный, +1 HP до макс. 3). Обновлён `Player.js` — добавлены таймеры `shieldTimer`/`doubleFireTimer`, динамический cooldown и отрисовка голубого кольца щита. Обновлён `WaveDirector.js` — спавн случайного усилителя каждые 12±3 сек с лимитом 3 на экране. Обновлён `CollisionManager.js` — добавлена коллизия `player` vs `powerup` с вызовом `applyTo()`, а также обработка щита при столкновении с врагом (поглощает удар и обнуляется). Повторный подбор Shield/DoubleFire сбрасывает таймер без стакания.

---

### [15.03.2026] Задача 5.2
**Результат:**
Создан файл `src/Boss.js` с классом `Boss` (`type = 'enemy'`, `hp = 30`, размер 200x160) и вложенным классом `BossProjectile` (`type = 'enemy_projectile'`). Босс входит сверху до Y=300 со скоростью 120 px/s, после остановки перемещается горизонтально синусоидой (±80px). Реализованы 3 типа атак: прямой залп (3 снаряда веером каждые 1.5 сек), круговой обстрел (8 снарядов 360° каждые 4 сек), прицельная ракета (1 снаряд в сторону игрока каждые 6 сек). Лимит 50 снарядов босса, очистка за экраном. Процедурная отрисовка корабля (корпус, броня, двигатели, мостик, турели) и HP-бар над боссом. При HP <= 0 — метод `onDeath()` с массивным взрывом частиц. Обновлён `WaveDirector.js`: таймер `totalTime`, через 30 сек прекращается спавн врагов и появляется босс (передаётся `particleSystem`). Обновлён `CollisionManager.js`: добавлена коллизия `enemy_projectile` vs `player` с поддержкой щита, распознавание босса через `onDeath`, при уничтожении — надпись «VICTORY» на canvas через оверлейную сущность. Обновлён `main.js`: `particleSystem` инициализируется перед `WaveDirector` и передаётся в его конструктор.

---

### [15.03.2026, 17:12:11] Задача 5.2
**Результат:**
Создан `src/Boss.js` с классом `Boss` (`type = 'enemy'`, `hp = 30`, 200x160) и `BossProjectile` (`type = 'enemy_projectile'`). Босс входит сверху до Y=300, затем движется горизонтально синусоидой (±80px). Три атаки: прямой залп (3 снаряда веером каждые 1.5с), круговой обстрел (8 снарядов 360° каждые 4с), прицельная ракета в игрока (каждые 6с). Лимит 50 снарядов босса, очистка за экраном. Процедурная отрисовка крейсера (корпус, броня, двигатели, мостик, турели) + HP-бар. При HP<=0 — `onDeath()` с массивным взрывом частиц. Обновлён `WaveDirector.js`: через 30 сек прекращается спавн врагов и появляется босс (принимает `particleSystem`). Обновлён `CollisionManager.js`: добавлена коллизия `enemy_projectile` vs `player` с поддержкой щита, при уничтожении босса — оверлейная сущность с надписью «VICTORY» на canvas. Обновлён `main.js`: порядок инициализации изменён — `particleSystem` создаётся до `WaveDirector` и передаётся ему.

---

