# Спринт 5: Утечки ресурсов

## Команда запуска для Ralph Runner
`claude -p "Прочитай этот файл (specs/005-utechki-resursov/spec.md). Найди ПЕРВУЮ невыполненную задачу (где стоит [ ]). Выполни ТОЛЬКО ЕЁ ОДНУ. Строго следуй правилам. ПОСЛЕ ВЫПОЛНЕНИЯ: 1) Обнови этот файл spec.md, отметив только ЭТУ выполненную задачу крестиком [x]. 2) В конце выведи маркер <promise>DONE</promise>." `

## Ссылки на контекст
- [Планирование](../../planning.md)

## Tasks

- [x] {{TASK:5.1}} Ротация логов web_server.log, crash.log, live_console_4.log
  **ПОДРОБНОСТИ:**
  - **Что сделать:**
    1. В `ralph-tracker-web.py` перед открытием лога: если >5MB → переименовать в `.old`.
    2. В `ralph4/ralph-overseer.js` при старте: если crash.log или live_console_4.log >10MB → обрезать до последнего 1MB.
  - **Критерии приёмки:** Логи не растут бесконечно при многодневной работе.

- [x] {{TASK:5.2}} Очистка словарей для удалённых проектов
  **ПОДРОБНОСТИ:**
  - **Что сделать:** В `ralph-tracker-web.py` при `/api/delete-project` очищать `_idea_queues[pid]`, `_idea_workers[pid]`, `LAUNCHED_PROCESSES[pid]` если они существуют.
  - **Критерии приёмки:** После удаления проекта нет утечки памяти через оставшиеся ссылки.

## Completion
- [ ] Все задачи выполнены
