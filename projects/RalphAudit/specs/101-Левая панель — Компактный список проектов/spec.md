# Спринт 101: Левая панель — Компактный список проектов

## Команда запуска для Ralph Runner
`claude -p "Прочитай этот файл (specs/101-Левая панель — Компактный список проектов/spec.md). Найди ПЕРВУЮ невыполненную задачу (где стоит [ ]). Выполни ТОЛЬКО ЕЁ ОДНУ. Строго следуй правилам из claude.md и применяй глобальные навыки (TDD, Debugging). ПОСЛЕ ВЫПОЛНЕНИЯ: 1) Обнови этот файл spec.md и корневой tasks.md, отметив только ЭТУ выполненную задачу крестиком [x]. 2) В конце выведи маркер <promise>DONE</promise>, а затем напиши блок <report>...</report>, внутри которого должен быть только строгий JSON с ключами: 'exact_task_name' (в точности скопируй строку задачи из файла), 'summary' (подробное описание того, что сделано), 'skills_used' (массив названий навыков, которые ты применил)."`

## Ссылки на контекст
- [Планирование](../../planning.md)
- [Правила](../../CLAUDE.md)
- [PRD](../../PRD.md)

## Tasks

- [x] {{TASK:101.1}} Перенести список проектов в левую панель и сделать компактный двухстрочный формат
  **ПОДРОБНОСТИ:**
  - **Что сделать:** Переместить `<div id="projects">` внутрь `.left-panel`. Изменить рендеринг проектов в функции `refresh()` (строки 1414-1448).
  - **Как сделать:**
    1. Новый HTML-шаблон для проекта (две строки):
       ```
       Строка 1: [📁] [Название]                    [Sonnet|Opus] [▶]
       Строка 2: [████████░░░] 5/12 (42%)
       ```
    2. Убрать `padding: 16px` → `padding: 10px 12px`. Уменьшить `margin-bottom: 12px` → `8px`.
    3. Первая строка: `.project-row-top { display: flex; align-items: center; justify-content: space-between; }` — слева иконка+имя+busy-бейдж, справа model-toggle+play/stop.
    4. Вторая строка: `.project-row-bottom { display: flex; align-items: center; gap: 8px; margin-top: 6px; font-size: 0.8em; }` — прогресс-бар + "5/12 (42%)".
    5. Статус-бейдж ("Запущен"/"Пауза") — цветная точка перед названием вместо отдельного бейджа: `<span style="width:8px;height:8px;border-radius:50%;background:${color}"></span>`.
    6. `.left-panel .projects-list { flex: 1; overflow-y: auto; padding: 12px; }` — скролл при большом количестве проектов.
    7. Прогресс-бар `.progress-bg` — переместить из absolute-позиции в flow: отдельный `<div>` внутри второй строки, `height: 4px; border-radius: 2px; background: var(--border);` с внутренним `<div style="width:${pct}%; background:var(--green);">`.
  - **Ограничения:** Контекстное меню (oncontextmenu) оставить без изменений. Все onclick-хэндлеры сохранить. `setProject(id)` — без изменений.
  - **Критерии приёмки:** Каждый проект занимает ~50px высоты (две строки). 10+ проектов → скролл в левой панели. Все кнопки работают: play/stop, model toggle, folder open, context menu.
- [x] {{TASK:101.2}} Перенести статус сервера и кнопки управления в нижнюю часть левой панели
  **ПОДРОБНОСТИ:**
  - **Что сделать:** Переместить блок "ВЕБ-СЕРВЕР: В СЕТИ" (строки 1183-1192) из `.header` в низ `.left-panel`.
  - **Как сделать:**
    1. HTML: `<div class="server-status">` — прибит к низу левой панели: `border-top: 1px solid var(--border); padding: 12px;`.
    2. Компактная версия: зелёная точка + "Сервер ОК" + кнопка перезапуска + кнопка консоли в одну строку.
    3. CSS: `.server-status { flex-shrink: 0; }` — не сжимается при переполнении.
  - **Ограничения:** `globalRestart()` и `showServerLogs()` — без изменений.
  - **Критерии приёмки:** Блок статуса сервера всегда виден внизу левой панели, даже при скролле списка проектов.

## Критерии завершения
- [ ] Все 2 невыполненных задач реализованы и протестированы.
- [ ] Файл `tasks.md` обновлен (поставлены [x] для завершенных пунктов).
